services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: trello-backend

    ports:
      - "8000:8000"

    env_file:
      - .env.production

    restart: unless-stopped